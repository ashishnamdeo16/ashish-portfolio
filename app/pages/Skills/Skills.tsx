"use client";

import SkillsShowcase from "@/app/components/skills/SkillsShowcase";

export default function Skills({ darkModeFlag }: { darkModeFlag?: boolean }) {
  void darkModeFlag;
  return <SkillsShowcase />;
}
